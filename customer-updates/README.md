# Kyron Customer Update Namespace

This namespace is intentionally outside the managed Kyron overlay so PantryPublisher never touches it.

Fixed updater URLs live under downloads; immutable archives live under releases; updater metadata is customer.json.
