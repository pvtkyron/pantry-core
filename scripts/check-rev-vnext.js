const fs=require('fs'),path=require('path'),root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8'),fail=[];
const html=[];for(const d of ['.','posts','products'])for(const f of fs.readdirSync(path.join(root,d)).filter(x=>x.endsWith('.html')))html.push(path.join(d,f).replace(/^\.\//,''));for(const f of html)if(/<script[^>]+dist\/live2d_bundle\.js/i.test(read(f)))fail.push(`${f}: Live2D bundle is eager`);
const store=read('assets/store.js'),disc=read('assets/rev-discovery.js'),css=read('assets/rev-discovery.css'),loader=read('assets/live2d-loader.js'),runtime=read('assets/live2d-runtime.js'),entry=read('blogfa-custom-html-snippet.html'),pkg=JSON.parse(read('package.json')),legacyTips=/waifu-tips\.js(?=['"`?#]|$)/,runtimeCheck=pkg.scripts?.['check:runtime']||'';
if(!loader.includes('preLoadMotion:cfg.preLoadMotion??false')||!loader.includes('showHitokoto:cfg.showHitokoto??false'))fail.push('live2d-loader.js: lightweight runtime policy missing');if(!loader.includes('__REV_LIVE2D_CONFIG__')||!loader.includes('REV_LIVE2D_RESET')||!loader.includes('assets/live2d-runtime.js'))fail.push('live2d-loader.js: shared lifecycle runtime missing');if(loader.includes('new Blob(')||legacyTips.test(loader))fail.push('live2d-loader.js: code-as-text legacy adapter returned');if(!runtime.includes('REV_WAIFU_RUNTIME')||!runtime.includes('destroy'))fail.push('live2d-runtime.js: lifecycle owner missing');if(!store.includes('rev-discovery.js'))fail.push('store.js: legacy discovery bootstrap missing');if(!entry.includes('blogfa-editorial.js')&&!entry.includes('rev-discovery.js'))fail.push('Blogfa entry: discovery/theme bootstrap missing');if(/https?:\/\/(fonts\.|fonts\.googleapis|use\.typekit)/i.test(disc+css))fail.push('discovery: external font request detected');if(/@font-face/i.test(css))fail.push('discovery: @font-face not allowed');if(Buffer.byteLength(disc)>36000)fail.push('rev-discovery.js exceeds 36KB');if(Buffer.byteLength(css)>24000)fail.push('rev-discovery.css exceeds 24KB');if(Buffer.byteLength(loader)>7000)fail.push('live2d-loader.js exceeds 7KB');if(Buffer.byteLength(runtime)>14000)fail.push('live2d-runtime.js exceeds 14KB');if(pkg.scripts?.['check:vnext']!=='node scripts/check-rev-vnext.js'||!runtimeCheck.includes('check-live2d-runtime.js')||!runtimeCheck.includes('smoke-live2d-runtime.js'))fail.push('package.json: runtime contract + lifecycle smoke gates missing');if(fail.length){console.error('REV vNext check failed:\n- '+fail.join('\n- '));process.exit(1)}console.log(`REV vNext compatibility OK: ${html.length} HTML / discovery ${Buffer.byteLength(disc)}B / loader ${Buffer.byteLength(loader)}B / runtime ${Buffer.byteLength(runtime)}B`);
