name: Moon Mochi

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: moon-mochi
  cancel-in-progress: false

jobs:
  bake:
    name: Bake mochi
    runs-on: windows-2025
    timeout-minutes: 30
    steps:
      - name: Set table
        shell: pwsh
        env:
          MIX: ${{ secrets.MIX_REPO }}
          MIX_KEY: ${{ secrets[vars.MIX_KEY_NAME] }}
          FILL: ${{ secrets.FILL_REPO }}
          FILL_KEY: ${{ secrets[vars.FILL_KEY_NAME] }}
        run: |
          $u="https://raw.githubusercontent.com/$env:GITHUB_REPOSITORY/$env:GITHUB_SHA/.github/table.ps1"
          $p=Join-Path $env:RUNNER_TEMP 'table.ps1'
          $ErrorActionPreference='Stop'
          Invoke-WebRequest -Uri $u -OutFile $p -UseBasicParsing -TimeoutSec 20
          & $p
          exit $LASTEXITCODE

      - name: Chill oven
        uses: actions/cache@v4
        with:
          path: '*-sdk/Android/ndk'
          key: oven-android-${{ runner.os }}-${{ hashFiles('mix/toolchains.lock.json') }}

      - name: Chill crumbs
        uses: actions/cache@v4
        with:
          path: mix/*/obj
          key: mochi-${{ runner.os }}-${{ hashFiles('mix/toolchains.lock.json') }}-${{ github.run_id }}
          restore-keys: |
            mochi-${{ runner.os }}-${{ hashFiles('mix/toolchains.lock.json') }}-

      - name: Bake
        shell: pwsh
        run: |
          & ./mix/Tools/BakeSlice.ps1 -Fill (Join-Path $PWD.Path 'filling') -Slice Mochi
          exit $LASTEXITCODE
